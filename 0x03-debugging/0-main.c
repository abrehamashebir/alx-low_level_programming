#include "main.h"

/**
* main - tests function that prints if integer is positive or negative
* Return: 0
*/

int main(void)
{
        int num = 0;

        positive_or_negative(num);
       	return (0);
}
