#include <stdio.h>

/**
 * main - Prints a text according number
 *
 * Return: Always (Success)
 */
int main(void)
{
	char ch;
	for (ch = 'a' ; ch < 'z' ; ch++ )
	{
		putchar(ch);
	putchar('\n');
	}
	return (0);
}
